<?php

$title = 'My Shopping Cart';
$this->headTitle($title);
?>

<h1><?php echo $this->escapeHtml($title); ?></h1>

<?php 
echo $this->navigation()
    ->menu('checkout_nav')
    ->setUlClass('nav navbar-nav navbar-checkout');
?>
<div class="clear-both"></div>

<?php
    $form = $this->form;
    $form->setAttribute('action', $this->url('order', array('action' => 'payment')));
    $form->prepare();

    echo $this->form()->openTag($form);
?>
<div class="checkout">
    <?php echo $this->formSubmit($form->get('submit')); ?>
</div>
<div>
    <h1>Choose Paymenttype</h1>
    <?php foreach($paymenttypes as $paymenttype): ?>
    <p>
        <h3>
            <input style="display: inline;" type='radio' name='paymenttype_id' value='<?php echo $paymenttype->getId(); ?>' /> 
            <?php echo $paymenttype->getName(); ?>
        </h3>
        <p class="payment-desc"><?php echo $paymenttype->getShortDescription(); ?> <a href="<?php echo $this->url('payment', array('action' => 'info', 'id' => $paymenttype->getId()))?>">more info...</a></p>
    </p>
    <?php endforeach; ?>
</div>

<div class="clear-both"></div>
<div class="checkout">
    <?php 
        echo $this->formSubmit($form->get('submit'));
        echo $this->formHidden($form->get('csrf'));
    ?>
</div>
<?php echo $this->form()->closeTag(); ?>
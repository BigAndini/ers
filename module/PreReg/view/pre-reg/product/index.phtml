<?php
// module/Catalogue/view/pre-registration/product/index.phtml:

$title = 'EJC 2015 Pre Registration';
$this->headTitle($title);
?>
<h1><?php echo $this->escapeHtml($title); ?></h1>

<?php 
foreach ($products as $product) : 
    if(count($product->getProductPrices()) == 0):
        continue;
    endif;
?>
<div class="product-container">
    <div class="add-to-cart">
        <p class="price">
            <?php #foreach($product->getProductPrices() as $price): ?>
                <?php echo $this->currencyFormat($product->getPrice()->getCharge(), 'EUR', null, 'de_DE'); ?><br />
            <?php #endforeach; ?>
        </p>
        <p class="product-detail">
            <a href="<?php echo $this->url('product', array('action'=>'view','id' => $product->getId()));?>"><?php echo $this->translate('product detail'); ?></a>
        </p>
    </div>
    <h3><?php echo $product->getName(); ?></h3>
    <p><?php echo $product->getShortDescription(); ?></p>
    
    <?php #$NextPriceDate = \DateTime::createFromFormat('Y-m-d H:i:s', $product->getNextPrice()->validFrom); ?>
    
</div>
<hr />

<?php endforeach; ?>
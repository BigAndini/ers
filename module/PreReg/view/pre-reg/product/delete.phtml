<?php

$title = _('Delete Item from shopping cart');
$this->headTitle($title);
?>
<h1><?php echo $this->escapeHtml($title); ?></h1>

<p><?php echo _('Are you sure that you want to delete'); ?> 
    <strong><?php echo $this->escapeHtml($item->getName()); ?></strong>
    <?php if($participant != null): ?>
    for <strong><?php echo $this->escapeHtml($participant->getFirstname().' '.$participant->getSurname()); ?></strong>
    <?php endif; ?>
    ?
</p>

<?php $obj = $form->getObject();
$form->setAttribute('action', $this->url('product', array('action' => 'delete', 'product_id' => $obj->getProductId(), 'item_id' => $obj->getId())))
      ->prepare();

echo $this->form()->openTag($form);

echo $this->formHidden($form->get('csrf'));
echo $this->formElement($form->get('submit')); ?>

<a href="<?php echo $this->url($breadcrumb->route, $breadcrumb->params, $breadcrumb->options); ?>">Cancel</a>

<?php echo $this->form()->closeTag();
